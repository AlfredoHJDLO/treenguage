import 'package:flutter/material.dart';

class VocabularioScreen extends StatelessWidget {
  const VocabularioScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Vocabulario Screen'));
  }
}